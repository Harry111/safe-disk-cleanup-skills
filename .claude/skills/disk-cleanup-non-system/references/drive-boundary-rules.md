# 盘符边界规则

用户必须指定目标盘符，例如 D:、E:、F:。

设用户指定盘符为 `<TARGET_DRIVE>`。

所有操作必须限制在：

```text
<TARGET_DRIVE>:\
```

禁止访问或处理：

```text
C:\
其他盘符
网络盘
未指定外接盘
```

除非用户重新明确指定新的盘符。

如果用户说“清理 D 盘”，不得擅自分析 C 盘或 E 盘。即使发现某些 Junction 指向其他盘，也不得跨过去继续处理。
